ServerTokens Prod
ServerSignature Off

ProxyPass "/cprosite/api" "${API_HOST}/api/"
ProxyPassReverse "/cprosite/api" "${API_HOST}/api/"

LogFormat "%h %{X-Forwarded-For}i %l %u %t \"%r\" %>s %b \"%{Referer}i\" \"%{User-Agent}i\"" combined
